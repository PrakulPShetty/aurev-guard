# AUREV Guard Frontend

Run the frontend (Vite + React) on port 5173.

Install and start:

```bash
cd frontend
npm install
npm run dev
```

Open http://localhost:5173 in your browser. Use a CIP-30 Cardano wallet browser extension (Nami, Eternl, Flint, Lace) or paste an address manually.
